services:
  traefik:
    image: traefik:v2.11
    container_name: 'laravel-13-start-traefik'
    ports:
      - '80:80'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command:
      - '--api.insecure=true'
      - '--providers.docker=true'
      - '--providers.docker.exposedbydefault=false'
      - '--entrypoints.web.address=:80'
    networks:
      - laravel-13-start

  mysql:
    container_name: 'laravel-13-start-mysql'
    image: 'mysql:8.0'
    ports:
      - '3306:3306'
    environment:
      MYSQL_DATABASE: '${DB_DATABASE:-laravel}'
      MYSQL_ROOT_HOST: '%'
      MYSQL_ROOT_PASSWORD: '${DB_PASSWORD:-secret}'
      MYSQL_ALLOW_EMPTY_PASSWORD: 1
    volumes:
      - 'laravel-13-start-mysql:/var/lib/mysql'
    networks:
      - laravel-13-start
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-psecret"]
      retries: 3
      timeout: 5s

  redis:
    container_name: 'laravel-13-start-redis'
    image: 'redis:alpine'
    ports:
      - '6379:6379'
    volumes:
      - 'laravel-13-start-redis:/data'
    networks:
      - laravel-13-start
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      retries: 3
      timeout: 5s

  app:
    container_name: 'laravel-13-start-app'
    image: laravel-13-start-app
    entrypoint: ['/app/entrypoint.sh']
    command: ["web"]
    build:
      context: .
      dockerfile: .docker/Dockerfile
      target: development
      args:
        WWWGROUP: '${WWWGROUP}'
        WWWUSER: '${WWWUSER}'
    depends_on:
      - traefik
      - mysql
      - test-mysql
      - redis
      - test-redis
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    ports:
      - '8080:8080'
      - '${VITE_PORT:-5173}:${VITE_PORT:-5173}'
    labels:
      - traefik.enable=true
      - traefik.http.routers.web.rule=Host(`laravel-13-start.localhost`)
      - traefik.http.routers.web.entrypoints=web
      - traefik.http.services.web.loadbalancer.server.port=8080
    environment:
      WWWUSER: '${WWWUSER}'
      COMPOSER_HOME: '/tmp/.composer'
      COMPOSER_AUTH: '${COMPOSER_AUTH}'
      XDEBUG_MODE: '${XDEBUG_MODE:-off}'
      XDEBUG_CONFIG: '${XDEBUG_CONFIG:-client_host=host.docker.internal}'
      PHP_OPCACHE_VALIDATE_TIMESTAMPS: '1'
      ENABLE_NPM_FRONTEND: '${ENABLE_NPM_FRONTEND:-true}'
    volumes:
      - '.:/var/www'
    networks:
      - laravel-13-start

  worker:
    container_name: 'laravel-13-start-worker'
    image: laravel-13-start-app
    entrypoint: ['/app/entrypoint.sh']
    command: ["worker"]
    build:
      context: .
      dockerfile: .docker/Dockerfile
      target: development
      args:
        WWWGROUP: '${WWWGROUP}'
        WWWUSER: '${WWWUSER}'
    depends_on:
      - app
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    environment:
      WWWUSER: '${WWWUSER}'
      COMPOSER_HOME: '/tmp/.composer'
      COMPOSER_AUTH: '${COMPOSER_AUTH}'
      XDEBUG_MODE: '${XDEBUG_MODE:-off}'
      XDEBUG_CONFIG: '${XDEBUG_CONFIG:-client_host=host.docker.internal}'
      PHP_OPCACHE_VALIDATE_TIMESTAMPS: '1'
    volumes:
      - '.:/var/www'
    networks:
      - laravel-13-start

  test-mysql:
    container_name: 'laravel-13-start-test-mysql'
    image: 'mysql:8.0'
    tmpfs: /var/lib/mysql
    ports:
      - '3307:3306'
    environment:
      MYSQL_DATABASE: '${DB_DATABASE:-laravel}'
      MYSQL_ROOT_HOST: '%'
      MYSQL_ROOT_PASSWORD: '${DB_PASSWORD:-secret}'
      MYSQL_ALLOW_EMPTY_PASSWORD: 1
    networks:
      - laravel-13-start
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-psecret"]
      retries: 3
      timeout: 5s

  test-redis:
    container_name: 'laravel-13-start-test-redis'
    image: 'redis:alpine'
    ports:
      - '6381:6379'
    networks:
      - laravel-13-start
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      retries: 3
      timeout: 5s

networks:
  laravel-13-start:
    name: laravel-13-start
    driver: bridge

volumes:
  laravel-13-start-mysql:
    driver: local
  laravel-13-start-redis:
    driver: local
